#include<stdio.h>
int main()
{
	//Write a C program to get input an integer of three digits, and find the sum of the individual digits
	int x, y, z, sum ;
	printf("enter tne number of x, y, z :\n");
	scanf("%d", &x);
	scanf("%d", &y);
	scanf("%d", &z);
	sum = x + y + z;
	printf("the sum is :%d", sum);





}